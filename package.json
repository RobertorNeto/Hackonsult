{
  "name": "pulso",
  "private": true,
  "version": "0.1.0",
  "description": "Pulso — monolito front (React/Vite) + back (Flask/SQLite).",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:front": "pnpm --dir frontend dev",
    "dev:back": "node scripts/run-backend.mjs",
    "setup": "node scripts/setup.mjs"
  }
}
